const listaEstado = require('./estados_cidades');

function getListaEstados(filtroEstado) {
    const uf = filtroEstado.estados.map((estado) => {
        
        return estado.sigla
        // 23,5
    })
    console.log(estado)
}

